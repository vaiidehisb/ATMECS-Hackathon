<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Predictive Maintenance</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #1a1a1a; /* Dark background */
            color: #e0e0e0; /* Light grey text */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        h1, h2 {
            color: #ffffff; /* White text for headers */
            text-align: center;
        }
        .container {
            background-color: #333333; /* Dark grey for container */
            margin: 20px;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 1000px;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        label {
            font-weight: 600;
            color: #b3b3b3; /* Grey for labels */
        }
        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #555; /* Darker border */
            border-radius: 5px;
            background-color: #444; /* Darker input background */
            color: #e0e0e0; /* Light grey text in inputs */
        }
        input[type="text"]::placeholder {
            color: #999; /* Lighter grey for placeholder */
        }
        input[type="submit"] {
            background-color: #44e661; /* Blue for submit button */
            color: rgb(0, 0, 0);
            padding: 12px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        input[type="submit"]:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
        .results {
            margin-top: 20px;
            padding: 20px;
            background-color: #214034; /* Blue for results box */
            color: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        }
        .result-box {
            margin-top: 10px;
        }
        .result-box strong {
            color: #FFC107; /* Yellow for result labels */
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Predictive Maintenance</h1>
        <form method="POST">
            <label for="air_temp">Air temperature [K]:</label>
            <input type="text" id="air_temp" name="air_temp" required pattern="^[0-9]+(\.[0-9]+)?$" title="Please enter a valid number.">

            <label for="process_temp">Process temperature [K]:</label>
            <input type="text" id="process_temp" name="process_temp" required pattern="^[0-9]+(\.[0-9]+)?$" title="Please enter a valid number.">

            <label for="rotational_speed">Rotational speed [rpm]:</label>
            <input type="text" id="rotational_speed" name="rotational_speed" required pattern="^[0-9]+(\.[0-9]+)?$" title="Please enter a valid number.">

            <label for="torque">Torque [Nm]:</label>
            <input type="text" id="torque" name="torque" required pattern="^[0-9]+(\.[0-9]+)?$" title="Please enter a valid number.">

            <label for="tool_wear">Tool wear [min]:</label>
            <input type="text" id="tool_wear" name="tool_wear" required pattern="^[0-9]+(\.[0-9]+)?$" title="Please enter a valid number.">

            <label for="type">Type:</label>
            <input type="text" id="type" name="type" required title="Please enter a valid type (e.g., L, M, H).">

            <input type="submit" value="Predict">
        </form>

        <% if (target) { %>
        <div class="results">
            <h2>Prediction Results</h2>
            <div class="result-box">
                <strong>Predicted Target:</strong> <%= target %>
            </div>
            <div class="result-box">
                <strong>Predicted Failure Type:</strong> <%= failure %>
            </div>
        </div>
        <% } %>
    </div>

</body>
</html>
